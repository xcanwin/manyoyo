# MANYOYO Flutter

这个目录预留给 Flutter 技术栈。

约定：

- Flutter 客户端工程根目录固定为 `apps/flutter/`。
- `macos/`、`windows/`、`ios/`、`android/` 等平台目录仅允许出现在本目录下。
- 不在仓库根目录创建 Flutter 平台目录，避免与主项目或其他技术栈冲突。

后续如果开始初始化 Flutter 工程，请在本目录内执行，例如：

```bash
cd apps/flutter
flutter create .
```
