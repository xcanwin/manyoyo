# Quality Checklist

- 文档标题为 `# 仓库协作指引`。
- 包含必需章节：协作偏好、项目结构、开发命令、编码风格、TDD、测试、提交与 PR、变更检查清单。
- 协作偏好包含：中文沟通、简洁回复、最小改动、不承诺时间线、不自动提交。
- 明确“优先小步改动，避免无关重构”。
- 明确 TDD 默认适用场景与文档例外。
- 命令来自仓库真实文件，不杜撰脚本。
- 提交规范来自近期 `git log`，非空泛模板。
- 文档不冗长，不重复，不含无效占位符。
